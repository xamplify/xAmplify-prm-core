pageable.page.parameter = page
pageable.page.missing = ${pageable.page.parameter} parameter is missing
pageable.page.invalid = ${pageable.page.parameter} value should be greater than 0


pageable.size.parameter = size
pageable.size.missing = ${pageable.size.parameter} parameter is missing
pageable.size.invalid = ${pageable.size.parameter} value should be greater than 0
pageable.size.max = ${pageable.size.parameter} value should not be greater than 48


pageable.sort.parameter = sort
pageable.sort.missing = ${pageable.sort.parameter} parameter is missing
pageable.sort.invalid = Invalid ${pageable.sort.parameter} option. It should be either ASC or DESC with sort column. For Example:- companyName,asc
pageable.sort.order.invalid = Invalid ${pageable.sort.parameter} order.It should be either ASC or DESC

